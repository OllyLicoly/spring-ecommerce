services:
  db:
    image: postgres:16
    container_name: spring-ecommerce-db
    environment:
      POSTGRES_DB: Shop
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
